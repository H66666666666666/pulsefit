<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbin.mapper.EmpMapper">
    <update id="updateBtId">

            update emp
                <!--<set>标签：自动处理字段末尾的逗号，避免 SQL 语法错误（例如最后一个字段后不会多出来逗号）-->
            <set>
                <!-- 仅当参数不为null时，才更新对应字段 -->
                <if test="username != null and username !=''">username = #{username},</if>
                <if test="password != null and password!=''">password = #{password},</if>
                <if test="name != null and name!=''">name = #{name},</if>
                <if test="gender != null">gender = #{gender},</if>
                <if test="phone != null and phone!=''">phone = #{phone},</if>
                <if test="job != null">job = #{job},</if>
                <if test="salary != null">salary = #{salary},</if>
                <if test="image != null and image!=''">image = #{image},</if>
                <if test="entryDate != null">entry_date = #{entryDate},</if>
                <if test="deptId != null">dept_id = #{deptId},</if>
                <!-- updateTime通常为更新时的当前时间，建议强制更新（无需判断） -->
                update_time = #{updateTime}
            </set>
            where id = #{id} <!-- id为更新的唯一标识，必须传入 -->
    </update>

    <delete id="deleteEmpById">
        delete from emp where id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>
    <delete id="deleteEmpExprById">
        delete from emp_expr where emp_id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>

    <select id="list" resultType="com.itbin.pojo.Emp">
        select emp.*,dept.name deptName from emp left join dept on dept_id=dept.id
            <where>
                <if test="name != null">
                    emp.name like concat('%',#{name},'%')
                </if>
                <if test="gender!=null">
                    and gender=#{gender}
                </if>
                <if test="begin!=null and end!=null">
                    and entry_date between #{begin} and #{end}
                </if>
            </where>
            order by emp.update_time desc
    </select>
    <!--定义resultMap-->
    <resultMap id="empResultMap" type="com.itbin.pojo.Emp">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="job" column="job"/>
        <result property="salary" column="salary"/>
        <result property="image" column="image"/>
        <result property="entryDate" column="entry_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <!--封装工作经历-->
        <collection property="exprList" ofType="com.itbin.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="empId" column="ee_empid"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="company" column="ee_company"/>
            <result property="job" column="ee_job"/>

        </collection>
    </resultMap>

    <select id="findByid" resultMap="empResultMap">
        select emp.*,
               ee.id ee_id,
               ee.emp_id ee_empid,
               ee.begin ee_begin,
               ee.end ee_end,
               ee.company ee_company,
               ee.job ee_job
                   from emp left join emp_expr ee on emp.id=ee.emp_id
        where emp.id=#{id}
    </select>
    <!--统计职位人数-->
    <select id="countEmpJobData" resultType="java.util.Map">
        select
            (case job when 1 then '班主任'
                    when 2 then '讲师'
                    when 3 then '学工主管'
                    when 4 then '教研主管'
                    when 5 then '咨询师'
                    else '其他' end) pos
             ,count(*) num  from emp group by job order by num
    </select>
    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender=1,'男','女') name
            ,count(*) value
        from emp group by gender
    </select>
    <select id="findAll" resultType="com.itbin.pojo.Emp">
        select * from emp
    </select>



    <select id="getUsernameAndPassword" resultType="com.itbin.pojo.Emp">
        select id,username,name from emp where username = #{username} and password = #{password}
    </select>
</mapper>