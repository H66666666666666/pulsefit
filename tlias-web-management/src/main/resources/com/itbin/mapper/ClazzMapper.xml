<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbin.mapper.ClazzMapper">
    <insert id="add">
        insert into clazz(name, room, begin_date, end_date, master_id, subject, create_time, update_time)
        values(#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>


    <update id="update">
        update clazz set
        name = #{name},
        room = #{room},
        begin_date = #{beginDate},
        end_date = #{endDate},
        master_id = #{masterId},
        subject = #{subject},
        update_time = #{updateTime}
        where id = #{id}
    </update>
    <delete id="deleteByid">
        delete from clazz where id=#{id}
    </delete>


    <select id="list" resultType="com.itbin.pojo.Clazz">
        select
        clazz.*,
        emp.name as masterName,
        case
        when DATE(NOW()) &lt; DATE(clazz.begin_date) THEN '未开班'
        when DATE(NOW()) > DATE(clazz.end_date) THEN '已结课'
        ELSE '在读中'
        END as status
        from clazz
        left join emp on emp.id = clazz.master_id
        <where>
            <if test="name != null">
                clazz.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and clazz.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by clazz.update_time desc
    </select>


    <select id="findByid" resultType="com.itbin.pojo.Clazz">
        select * from clazz where id = #{id}
    </select>
    <select id="findAll" resultType="com.itbin.pojo.Clazz">
        select * from clazz
    </select>
</mapper>